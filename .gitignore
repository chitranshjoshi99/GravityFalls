# Godot 4
.godot/
/android/
*.translation

# export_presets.cfg IS tracked: tracker row 0.2 makes the tests//tools/ export
# filter a day-one deliverable, and this preset carries no credentials
# (codesign and notarization are both off).

# Exports / builds
build/
exports/
*.app/
*.dmg
*.zip
*.wasm

# Baked placeholder audio (regenerated at startup, see docs/05 §7)
art/generated/
audio/generated/

# macOS
.DS_Store
.AppleDouble

# Editors
.vscode/
.idea/
*.swp
